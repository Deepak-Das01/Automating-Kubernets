---
- name: first kubernetes playbook
  hosts: master
  tasks:

    - name: Create a k8s namespace
      kubernetes.core.k8s:
        name: testing
        api_version: v1
        kind: Namespace
        state: present

    - name: Create a Deployment by reading the definition from a local file
      kubernetes.core.k8s:
        state: present
        namespace: testing  
        src: /home/deepak/ngnix.yaml
      
